mkdir api
cd api
npm init -y

# Instala o Express (dependência de produção)
npm install express

# Instala o TypeScript, as tipagens e a ferramenta de hot-reload (dependências de desenvolvimento)
npm install -D typescript @types/express @types/node ts-node-dev